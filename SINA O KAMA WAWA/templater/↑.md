<%*
// 等待一段时间，确保页面已完全加载
await new Promise(resolve => setTimeout(resolve, 500));

// 查找编号列表的第一行
let firstRow = document.querySelector('.custom-table tbody tr:first-child');

if (firstRow) {
    // 滚动到第一行
    firstRow.scrollIntoView({ behavior: 'smooth', block: 'start' });
    new Notice('已滚动到编号列表的第一行');
} else {
    new Notice('未找到编号列表的第一行');
}
%>