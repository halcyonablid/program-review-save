<%*
// 等待一段时间，确保页面已完全加载
await new Promise(resolve => setTimeout(resolve, 500));

// 查找编号列表的最后一行
let lastRow = document.querySelector('.custom-table tbody tr:last-child');

if (lastRow) {
    // 滚动到最后一行
    lastRow.scrollIntoView({ behavior: 'smooth', block: 'end' });
    new Notice('已滚动到编号列表的最后一行');
} else {
    new Notice('未找到编号列表的最后一行');
}
%>