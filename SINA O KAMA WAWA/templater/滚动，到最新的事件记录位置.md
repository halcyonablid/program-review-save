<%*
async function findAndScrollToNote() {
    // 获取当前文件的内容
    const currentFile = tp.file.find_tfile(tp.file.title);
    const content = await app.vault.read(currentFile);
    
    // 解析YAML获取目标文件名
    let targetFileName = "";
    if (content.startsWith('---')) {
        const yamlEndIndex = content.indexOf('---', 3);
        if (yamlEndIndex !== -1) {
            const yamlContent = content.substring(0, yamlEndIndex);
            const match = yamlContent.match(/最新的事件记录的笔记名:\s*(.*)/);
            if (match) {
                targetFileName = match[1].trim();
            }
        }
    }

    if (!targetFileName) {
        new Notice("未在当前文件的YAML中找到目标文件名");
        return;
    }

    // Wait for the page to load
    await new Promise(resolve => setTimeout(resolve, 200));

    try {
        // Find the custom table
        const customTable = document.querySelector('.custom-table tbody');
        if (!customTable) {
            throw new Error('未找到自定义表格');
        }

        // Find the row with the target filename
        const targetRow = Array.from(customTable.querySelectorAll('tr')).find(row => {
            const link = row.querySelector('.filename-column a');
            return link && link.textContent === targetFileName;
        });

        if (targetRow) {
            // Set up IntersectionObserver
            const observer = new IntersectionObserver((entries) => {
                if (!entries[0].isIntersecting) {
                    targetRow.scrollIntoView({ behavior: 'smooth', block: 'center' });
                    observer.disconnect();
                }
            }, { threshold: 1.0 });

            observer.observe(targetRow);

            // Trigger initial scroll if not in view
            if (!isElementInViewport(targetRow)) {
                const scrollContainer = document.querySelector('.workspace-leaf-content');
                if (scrollContainer) {
                    const containerHeight = scrollContainer.clientHeight;
                    const newScroll = Math.random() < 0.5 ? containerHeight / 4 : containerHeight * 3 / 4;
                    scrollContainer.scrollTo({
                        top: newScroll,
                        behavior: 'smooth'
                    });
                }
            }

            const rowIndex = Array.from(customTable.children).indexOf(targetRow) + 1;
            new Notice(`已找到并滚动到目标笔记：${targetFileName}（第 ${rowIndex} 行）`);
        } else {
            new Notice(`在编号列表中未找到目标笔记：${targetFileName}`);
        }
    } catch (error) {
        console.error('Error:', error);
        new Notice(`查找过程中发生错误：${error.message}`);
    }
}

function isElementInViewport(el) {
    const rect = el.getBoundingClientRect();
    return (
        rect.top >= 0 &&
        rect.left >= 0 &&
        rect.bottom <= (window.innerHeight || document.documentElement.clientHeight) &&
        rect.right <= (window.innerWidth || document.documentElement.clientWidth)
    );
}

// Run the main function
await findAndScrollToNote();
%>