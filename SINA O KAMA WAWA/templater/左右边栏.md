<%*
let rightSidebar = app.workspace.rightSplit.collapsed;

if (rightSidebar) {
    // 如果右侧边栏关闭，则打开
    app.workspace.rightSplit.expand();
} else {
    // 如果右侧边栏打开，则关闭
    app.workspace.rightSplit.collapse();
}
%>



